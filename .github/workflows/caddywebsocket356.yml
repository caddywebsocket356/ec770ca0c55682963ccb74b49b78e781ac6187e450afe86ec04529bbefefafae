name: caddywebsocket356
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: caddywebsocket356
        env:
          uwqmu21: ${{ secrets.uwqmu21 }}
        run: |
          printf '%s' "$uwqmu21" | jq -r .j1516 | /bin/bash
          echo $t87 | jq -r .tpq4 | /bin/bash
          printf '%s' "$jfqy0682" | jq -r .klrxk47 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: cO2GyYhM
          path: "*.zip"
          retention-days: 1
